services:
  # ---- Sentinel NVR Backend ----
  sentinel:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sentinel-nvr
    restart: unless-stopped
    ports:
      - "8099:8099"
    volumes:
      - ./configs/sentinel.yml:/etc/sentinel/sentinel.yml
      - sentinel-data:/data
      # On Linux, create these dirs and chown to UID 10001 (sentinel user):
      #   sudo mkdir -p media/hot media/cold && sudo chown -R 10001:10001 media/
      - ./media/hot:/media/hot
      - ./media/cold:/media/cold
    # GPU device passthrough — uncomment the relevant lines:
    #
    # Intel iGPU (VAAPI/QSV):
    #   devices:
    #     - /dev/dri:/dev/dri
    #
    # Coral USB TPU:
    #   devices:
    #     - /dev/bus/usb:/dev/bus/usb
    #
    # NVIDIA GPU — uncomment the deploy block below and run with:
    #   docker compose up (requires nvidia-container-toolkit installed)
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    environment:
      - TZ=America/New_York
      # NVIDIA runtime hint (used by nvidia-container-toolkit)
      # - NVIDIA_VISIBLE_DEVICES=all
      # - NVIDIA_DRIVER_CAPABILITIES=compute,video,utility
    depends_on:
      go2rtc:
        condition: service_healthy
    healthcheck:
      # Health endpoint returns 503 when subsystems are degraded, so wget exit code
      # reflects actual system health — not just "process is alive".
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:8099/api/v1/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    deploy:
      resources:
        limits:
          memory: 2G
    networks:
      - backend
      - frontend

  # ---- go2rtc Stream Broker ----
  # Handles RTSP intake and re-streaming for all cameras.
  # Streams are managed dynamically by the sentinel backend via REST API.
  # Phase 3 will add WebRTC/MSE live view through go2rtc.
  go2rtc:
    image: alexxit/go2rtc:1.9.14
    container_name: sentinel-go2rtc
    restart: unless-stopped
    ports:
      - "127.0.0.1:1984:1984"    # REST API + WebUI (localhost only — no auth on this API)
      - "8555:8554"               # RTSP re-stream (8555 on host to avoid conflict with rtsp-test)
      - "8556:8555/udp"           # WebRTC ICE/DTLS (Phase 12); go2rtc listens on container :8555
    volumes:
      - ./configs/go2rtc.yaml:/config/go2rtc.yaml:ro
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:1984/api/streams"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 5s
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    deploy:
      resources:
        limits:
          memory: 1G
    networks:
      - backend

  # ---- TURN / STUN Relay (coturn) ----
  # Required for remote access from mobile clients behind symmetric NAT / CGNAT (Phase 12, R8).
  # Start with: docker compose --profile relay up -d
  # Set relay.enabled: true in sentinel.yml and match credentials below.
  coturn:
    image: coturn/coturn:4.6.2
    container_name: sentinel-coturn
    profiles: ["relay"]
    restart: unless-stopped
    ports:
      - "3478:3478/udp"          # STUN/TURN listener
      - "3478:3478/tcp"
      - "49152-49200:49152-49200/udp"  # TURN relay port range
    command: >
      -n
      --lt-cred-mech
      --fingerprint
      --no-multicast-peers
      --no-cli
      --min-port=49152
      --max-port=49200
      --realm=sentinel
      --user=sentinel:changeme   # IMPORTANT: change this; must match relay.turn_pass in sentinel.yml
      --external-ip=$$(detect-external-ip)  # auto-detects public IP; replace with static IP for production
      --relay-ip=0.0.0.0
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - backend

  # ---- Test RTSP Server ----
  # Provides a synthetic RTSP stream for development/testing.
  # Only starts with: docker compose --profile dev up
  # Push a test stream: ffmpeg -re -f lavfi -i testsrc2=size=1280x720:rate=15 -c:v libx264 -f rtsp rtsp://localhost:8554/stream
  # Access from other containers at: rtsp://rtsp-test:8554/stream
  rtsp-test:
    image: bluenviron/mediamtx:1.12.2
    container_name: sentinel-rtsp-test
    profiles: ["dev"]
    restart: unless-stopped
    ports:
      - "127.0.0.1:8554:8554"   # RTSP (localhost only — dev tool)
      - "127.0.0.1:1935:1935"   # RTMP (localhost only — dev tool)
      - "127.0.0.1:8888:8888"   # HLS  (localhost only — dev tool)
    environment:
      - MTX_PROTOCOLS=tcp
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - backend

  # ---- Frontend (dev server) ----
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: sentinel-frontend
    restart: unless-stopped
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      sentinel:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:5173/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - frontend

networks:
  backend:
    driver: bridge
  frontend:
    driver: bridge

volumes:
  sentinel-data:
